use std::net::TcpListener;

use crate::base::properties::Properties;

struct Server{
    
}

impl Server {
    pub fn new(){
    
    }
    pub fn run(){
        // let listener=TcpListener::bind(format!("{}:{}",Properties::get(key, "127.0.0.1"),));    
    }
}